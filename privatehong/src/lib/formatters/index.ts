// TODO: Telegram message formatters
// Utilities for formatting messages for Telegram
//
// Functions to implement:
//   - formatNumber(n: number): string  → "1,234,567"
//   - formatPercent(n: number): string → "+1.23%" or "-0.45%"
//   - formatKRW(n: number): string     → "₩1,234,567"
//   - formatUSD(n: number): string     → "$1,234.56"
//   - formatDate(d: Date): string      → "2024년 3월 15일 (금)"
//   - escapeMarkdown(s: string): string → escape MarkdownV2 special chars

export {};
